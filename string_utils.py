# Function A: chooses multiplication or addition based on comparison
def something_incredible_and_good(x: int, y: int) -> int:
    if x > y:
        return x * y
    else:
        return x + y

# Function B: normalizes a string
def something_normal(s: str) -> str:
    return s.strip().lower()
